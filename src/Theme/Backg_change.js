import React, {useState, useRef } from 'react'


const BgChange = () => {
     // TODO: this should be applied in history backgound and ....
       const [bg, setbg] = useState(null);
        
       const handleBackgroundChange = () => {
         
       }

     return(
          <>
          </>
     );
}

export default BgChange